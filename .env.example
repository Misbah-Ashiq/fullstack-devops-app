# Copy to .env and edit as needed
MYSQL_ROOT_PASSWORD=rootpass
DB_NAME=appdb
DB_USER=appuser
DB_PASSWORD=apppass
# Frontend calls http://localhost:5000 by default; override if needed:
# VITE_API_BASE=http://your-server:5000